using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossScrpit : MonoBehaviour
{
    public Animator animator;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //if (animator.GetParameter(4).Equals(true))
        //{
        //    Debug.Log("true");
        //    if (!animator.GetCurrentAnimatorStateInfo(0).IsName("BossIddle2"))
        //    {
        //        animator.SetBool("red", false);
        //        animator.SetBool("blue", false);
        //        animator.SetBool("green", false);
        //    }
        //}
        //else
        //{
        //    if (!animator.GetCurrentAnimatorStateInfo(0).IsName("BossIddle1"))
        //    {
        //        animator.SetBool("red", false);
        //        animator.SetBool("blue", false);
        //        animator.SetBool("green", false);
        //    }
        //}
    }
}
